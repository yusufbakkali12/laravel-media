# Laravel Media Package

Simple polymorphic media library for Laravel.

## Installation

1. Add repository (if local):

```json
"repositories": [
    {
        "type": "path",
        "url": "../laravel-media"
    }
]
